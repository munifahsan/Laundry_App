<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tiunida.laundry0">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeDua">
        <activity android:name=".orderDetail.ui.OrderDetailActivity" />

        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".order.express.ui.ExpressActivity" />
        <activity android:name=".order.kilat.ui.KilatActivity" />
        <activity android:name=".editProfile.ui.EditProfileActivity" />
        <activity android:name=".main.MainActivity" />
        <activity android:name=".login.ui.LoginActivity" />
        <activity android:name=".order.biasa.ui.BiasaActivity" />
        <activity android:name=".register.ui.RegisterActivity" />
        <activity android:name=".forgetPass.ForgetPassActivity" />
        <activity android:name=".setup.ui.SetupActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>